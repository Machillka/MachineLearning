#include "MVector.h"

template<typename T >
void MVector<T>::AddItem(T value)
{
	
}

template<typename T >
MVector<T> MVector<T>::Dot(MVector<T> dotVec)
{
	// sizeA not equal to sizeB
	if (this->shape != dotVec.shape)
	{
		//!TODO use exception 
		printf("Shape Error")
		return null;
	}

	for()

	return this;
}